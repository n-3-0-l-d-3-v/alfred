# Local development against Postgres, so you exercise the same database engine
# the deployment uses. Plain `uvicorn` on SQLite still works for quick runs —
# see apps/api/README.md.

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: alfred
      POSTGRES_PASSWORD: alfred
      POSTGRES_DB: alfred
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U alfred"]
      interval: 5s
      retries: 10

  api:
    build: ./apps/api
    depends_on:
      db:
        condition: service_healthy
    environment:
      ALFRED_DATABASE_URL: postgresql+psycopg://alfred:alfred@db:5432/alfred
      # Local-only values. The deployed app gets these from Fly secrets.
      ALFRED_JWT_SECRET: local-development-secret-do-not-use-in-production
      ALFRED_DEV_AUTH_ENABLED: "true"
    ports:
      - "8000:8000"

volumes:
  pgdata:
