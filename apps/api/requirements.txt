fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
pyyaml>=6.0
anthropic>=0.40
# Ecosystem MCP server (apps/api/alfred/mcp_server.py) — same major version
# the sibling Friday agent uses (mcp.server.mcpserver.MCPServer).
mcp>=2.0.0
pytest>=8.0
httpx>=0.27
pyjwt>=2.8
alembic>=1.13

# Postgres driver. SQLite needs nothing extra, so local dev works without it,
# but a deployed ALFRED_DATABASE_URL of postgresql+psycopg://... requires it.
psycopg[binary]>=3.1

# Multi-language static analysis. Optional at runtime — if these are missing,
# Python analysis still works and other languages degrade to an honest
# "not supported" signal rather than crashing (see analysis/registry.py).
tree-sitter>=0.23
tree-sitter-python>=0.23
tree-sitter-cpp>=0.23
tree-sitter-java>=0.23
tree-sitter-javascript>=0.23
tree-sitter-go>=0.23
